$(document).ready(function() {
	/*var task_report_modal = new jBox('Modal', {
		width: $(window).innerWidth()
	}); 
	task_report_modal.open();
	task_report_modal.setContent('<img src="/img/ajax-loader-3.svg" style="display: block; margin: 30px auto 20px auto;">');
	$.post("/php_dependancies/daily_tasks.php", {get_report: true}, function(data) {
		if(data != null && data.length > 0){
			task_report_modal.setContent("<div id='tasks_report'>"+data+"</div>");
		}
	});*/
});